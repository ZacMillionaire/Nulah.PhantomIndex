<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Nulah.PhantomIndex.Core.Controls">

    <Style TargetType="{x:Type local:NulahFancyBorder}" BasedOn="{StaticResource {x:Type UserControl}}">
        <!--<Setter Property="Label" Value="Label" />
            <Setter Property="HintForeground" Value="#FFB5B5B5" />-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NulahFancyBorder}">
                    <Grid>
                        <Polygon x:Name="FancyBorderPolygon">
                            <Polygon.Style>
                                <Style TargetType="Polygon">
                                    <Setter Property="Fill" Value="White" />
                                    <Setter Property="Stretch" Value="Fill" />
                                    <Setter Property="Stroke" Value="Black" />
                                </Style>
                            </Polygon.Style>
                        </Polygon>
                        <ContentPresenter/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>