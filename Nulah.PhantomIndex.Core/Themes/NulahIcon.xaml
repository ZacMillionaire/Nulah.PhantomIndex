<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             
                    xmlns:local="clr-namespace:Nulah.PhantomIndex.Core.Controls">

    <Style TargetType="{x:Type local:NulahIcon}" BasedOn="{StaticResource {x:Type UserControl}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NulahIcon}">
                    <TextBlock Margin="5,0" 
                               FontSize="{TemplateBinding FontSize}" 
                               SnapsToDevicePixels="True" 
                               VerticalAlignment="Center" 
                               FontFamily="Segoe MDL2 Assets" 
                               Text="{TemplateBinding IconGlyph}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

</ResourceDictionary>